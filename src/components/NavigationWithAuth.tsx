'use client'

// Kept as a thin re-export so existing imports keep working. The canonical,
// permission-aware navigation lives in Navigation.tsx and is used on every page.
export { default } from './Navigation'
